<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.naresh.demoproject_1.NavigationDrawerActivity"
    >
    <TextView
        android:id="@+id/text_detail_not_available"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingLeft="@dimen/padding_left_text_detail_not_available"
        android:text="@string/string_detail_not_found"
        android:textSize="20sp"
        android:visibility="gone"
        android:textStyle="bold"
        android:layout_centerVertical="true" />

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="visible"
        android:id="@+id/list_question_profile_fragment"/>
    <ProgressBar
        android:id="@+id/progressbar_profile_fragment"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone" />
    <TextView
        android:id="@+id/text_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/progress_title"
        android:textSize="10sp"
        android:layout_above="@+id/progressbar_profile_fragment"
        android:layout_centerInParent="true"
        android:visibility="gone"/>

</RelativeLayout>
